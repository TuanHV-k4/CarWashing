import { useState } from 'react'
import { Link } from 'react-router'
import { ArrowLeft, Mail, Droplets } from 'lucide-react'
import { api, ApiError } from '../../shared/api/client'
import loginBackdrop from '../../Mondlicht-Studios-Porsche-Zhanna-Travkina3.jpg'
export function ForgotPasswordPage() { const [email, setEmail] = useState(''); const [error, setError] = useState(''); const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); async function submit(e: React.FormEvent) { e.preventDefault(); setError(''); if (!email) { setError('Vui lòng nhập email.'); return }; setLoading(true); try { await api.forgotPassword(email); setSent(true) } catch (c) { setError(c instanceof ApiError ? c.message : 'Không thể gửi yêu cầu.') } finally { setLoading(false) } } return <main className="login-page register-page"><section className="login-art" aria-hidden="true"><img className="login-backdrop" src={loginBackdrop} alt="" /><div className="login-art-copy"><p>AutoWash Pro</p><strong>Lấy lại quyền truy cập<br />một cách an toàn.</strong></div></section><section className="login-panel"><Link className="brand login-panel-brand" to="/"><span className="brand-mark"><Droplets size={20} /></span>AutoWash <em>Pro</em></Link><div className="login-card register-card"><Link className="back-link" to="/login"><ArrowLeft size={16} /> Quay lại đăng nhập</Link>{sent ? <div className="register-success"><Mail size={38} /><h1>Kiểm tra hộp thư của bạn.</h1><p className="muted">Nếu email đã được đăng ký, chúng tôi đã gửi hướng dẫn đặt lại mật khẩu.</p><Link className="button button-primary login-submit" to="/login">Về trang đăng nhập</Link></div> : <><p className="eyebrow"><span /> Hỗ trợ tài khoản</p><h1>Quên mật khẩu?</h1><p className="muted">Nhập email để nhận liên kết đặt lại mật khẩu.</p><form onSubmit={submit}><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="email" value={email} onChange={e => setEmail(e.target.value)} required />{error && <p className="form-error" role="alert">{error}</p>}<button className="button button-primary login-submit" disabled={loading}>{loading ? 'Đang gửi…' : 'Gửi liên kết'}</button></form></>}</div></section></main> }
